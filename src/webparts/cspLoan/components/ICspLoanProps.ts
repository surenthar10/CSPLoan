export interface ICspLoanProps {
  description: string;
  isDarkTheme: boolean;
  environmentMessage: string;
  hasTeamsContext: boolean;
  userDisplayName: string;
  context: any; // You can replace 'any' with the specific type of context if available
}
